# VS Marketplace / OpenVSX publish tokens for vhash-vscode-ext.
# Copy to: <config_root>/xapp-keys/apps/vhash-vscode-ext/vhash-vscode-ext-secrets.yml
# <config_root> = /etc/xapp-config (server) or ~/.config/xapp-config (user)

publish:
  default_profile: default
  profiles:
    default:
      azure_devops_pat_for_vsce: ''   # Azure DevOps PAT (vsce publish)
      openvsx_pat: ''                 # OpenVSX access token
